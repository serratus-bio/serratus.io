version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 14
        - npm ci
    build:
      commands:
        - nvm use 14
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
