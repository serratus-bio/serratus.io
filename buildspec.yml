version: 0.2

phases:
  build:
    commands:
      - cd frontend
      - npm install
      - npm run build
artifacts:
  files:
    - dist/**/*
    - backend/**/*
    - requirements.txt
    - .ebextensions/**/*
