const colors = require('tailwindcss/colors')

module.exports = {
    theme: {
        colors: {
            black: colors.black,
            white: colors.white,
            gray: colors.blueGray,
            blue: colors.sky,
        },
    },
}
